# apps/admin/.env — copié tel quel dans la sortie standalone au build,
# donc à renseigner AVANT `pnpm --filter @jokko/admin run build`.
JOKKO_API_URL=https://api.scoliaa.com/api
DASHBOARD_PUBLIC_URL=https://dashboard.scoliaa.com
