{
  "name": "@jokko/e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Tests bout-en-bout Playwright (vitrine + dashboard + API).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "typecheck": "tsc --noEmit",
    "install:browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
