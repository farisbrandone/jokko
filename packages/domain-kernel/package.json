{
  "name": "@jokko/domain-kernel",
  "version": "0.0.0",
  "private": true,
  "description": "Primitives DDD partagées (Result, Entity, AggregateRoot, ValueObject, DomainEvent).",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"(domain-kernel) pas de tests pour l'instant\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
