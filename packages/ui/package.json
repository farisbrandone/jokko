{
  "name": "@jokko/ui",
  "version": "0.0.0",
  "private": true,
  "description": "Tokens de design et utilitaires partagés (storefront, dashboard, admin).",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": { "types": "./src/index.ts", "default": "./src/index.ts" },
    "./tokens.css": "./src/tokens.css"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "echo \"(ui) rien à compiler pour l'instant\" && exit 0",
    "test": "echo \"(ui) pas de tests\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
