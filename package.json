{
  "name": "jokko",
  "version": "0.0.0",
  "private": true,
  "description": "Jokko — plateforme de boutiques sociales (monorepo)",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22.11 <23"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "build:packages": "pnpm --filter \"./packages/*\" run build",
    "dev": "pnpm run build:packages && concurrently -k -n api,store,dash,admin -c blue,green,magenta,yellow \"pnpm --filter @jokko/api run start:dev\" \"pnpm --filter @jokko/storefront run dev\" \"pnpm --filter @jokko/dashboard run dev\" \"pnpm --filter @jokko/admin run dev\"",
    "dev:api": "pnpm run build:packages && pnpm --filter @jokko/api run start:dev",
    "dev:storefront": "pnpm run build:packages && pnpm --filter @jokko/storefront run dev",
    "dev:dashboard": "pnpm run build:packages && pnpm --filter @jokko/dashboard run dev",
    "dev:admin": "pnpm run build:packages && pnpm --filter @jokko/admin run dev",
    "db:migrate": "pnpm --filter @jokko/api run db:migrate",
    "seed": "pnpm --filter @jokko/api run seed",
    "lint": "eslint .",
    "format": "prettier --write .",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck",
    "infra:up": "docker compose -f infra/docker/compose.yaml up -d",
    "infra:down": "docker compose -f infra/docker/compose.yaml down",
    "infra:logs": "docker compose -f infra/docker/compose.yaml logs -f"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "concurrently": "^9.1.2",
    "eslint": "^9.17.0",
    "eslint-config-prettier": "^9.1.0",
    "globals": "^15.14.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.1"
  }
}
